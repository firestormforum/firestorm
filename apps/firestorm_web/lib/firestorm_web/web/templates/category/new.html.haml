%h2 New Category

- form_for @changeset, category_path(@conn, :create), [class: 'new-category'], fn f ->
  %label
    Title: #{text_input f, :title}

  = hidden_input f, :parent_id
  = submit "Create Category"
